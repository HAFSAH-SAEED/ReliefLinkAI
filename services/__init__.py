"""Service layer for ReliefLink AI integrations.

This package keeps external service access separate from Streamlit UI code so
the application can grow without coupling pages directly to API clients.
"""
